================================================================
PROJECT: ClickWork - Document Library Platform
================================================================

----------------------------------------------------------------
DATE: April 28th, 2026
AUTHOR: MiguelAGDev
SESSION TIME: ~2 hours
ACTIONS: Backend scaffolding and base configuration
----------------------------------------------------------------

[~0:00 - 0:30] Project Initialization
- Initialized Node.js project and installed dependencies
- Created folder structure (src/config, controllers, middleware, models, routes)

[~0:30 - 1:00] Express Server (backend/index.js)
- Configured CORS, dotenv and express.json()
- Imported and mounted all route modules under /api prefix
- Imported centralized error handler
- Server starts on PORT defined in .env (default 3000)
- Fixed ES module error: added "type": "module" to package.json

[~1:00 - 1:30] Environment and Config
- Configured .env with DB, JWT, Nodemailer and frontend variables
- Generated .env.example with all keys and empty sensitive values
- Confirmed .env is in .gitignore

[~1:30 - 2:00] Middleware and Routes
- Created src/middlewares/errorHandler.js — centralized JSON error response
  * Returns status, message and stack trace (dev only)
- Stubbed all 5 route files: authRoutes, userRoutes, announcementRoutes,
  applicationsRoutes, adminRoutes
- Tested server: running successfully on port 3000
- Two commits pushed to main branch

----------------------------------------------------------------
DATE: April 29th, 2026
AUTHOR: MiguelAGDev
SESSION TIME: ~1 hour
ACTIONS: Mailer config and email service
----------------------------------------------------------------
[~0:00 - 0:20] Mailer Transport (backend/src/config/mailer.js)
- Created Nodemailer SMTP transporter using .env credentials
- Auto-detects SSL vs TLS based on port (465 = SSL, 587 = STARTTLS)
- Added dev-only transporter.verify() for startup connection check
[~0:20 - 1:00] Email Service (backend/src/services/emailService.js)
- Created shared HTML base template (buildTemplate) for consistent styling
- Implemented sendVerificationEmail — sent on register with /verify/:token link
- Implemented sendWelcomeEmail — sent after account is verified
- Implemented sendRollMeEmail — Roll Me feature, attaches student PDF CV
  and sends it directly to the company's contact email
- Implemented sendPasswordResetEmail — scaffold for future reset flow
- sendApplicationStatusEmail commented out (not in use — no in-platform
  application status flow for now, Roll Me handles company contact)
- Fixed two bugs in mailer.js: EMAIL_HOST -> MAIL_HOST, proccess -> process

----------------------------------------------------------------
DATE: May 1st, 2026
AUTHOR: MiguelAGDev & AzucenaRF
SESSION TIME: ~1 hour
ACTIONS: Permissions system design and implementation
----------------------------------------------------------------
[~0:00 - 1:00] Permissions Config (backend/src/config/permissions.js)
- Designed bitmask-based permission system using hexadecimal constants
- Created full PERMISSIONS dictionary with 30 permissions across 5 groups:
  PUBLIC, AUTH, STUDENT/INTERN/GRADUATE, COMPANY, ADMIN
- Defined role permission arrays: STUDENT_PERMISSIONS, INTERN_PERMISSIONS,
  GRADUATE_PERMISSIONS, COMPANY_PERMISSIONS, ADMIN_PERMISSIONS
- Built ROLE_MASK object — each role mask computed via bitwise OR reduce
- Added hasPermission(userMask, permission) helper for middleware use
- VIEW_REPORTS included in ADMIN_PERMISSIONS — TODO: pending implementation
- Fixed bug: && → & in hasPermission (logical vs bitwise AND)


----------------------------------------------------------------
DATE: May 2nd, 2026
AUTHOR: MiguelAGDev
SESSION TIME: ~1 hour
ACTIONS: Database schema and connection setup
----------------------------------------------------------------
[~0:00 - 1:00] Database Setup
- Ran database schema (database.sql) on local MySQL instance
- Created clickwork database with 11 tables:
  careers, app_user, company, student, graduate, intern,
  job_posting, application, role_history, audit_log, permissions
- Configured MySQL2 connection pool (backend/src/config/db.js)
- Fixed timezone warning: replaced 'Z' with '+00:00' (UTC)
- Verified connection successfully with node -e "require('./src/config/db')"
- Committed: database.sql + db.js

----------------------------------------------------------------
DATE: May 6th, 2026
AUTHOR: MiguelAGDev & AzucenaRF
SESSION TIME: ~2 hours
ACTIONS: Models — studentModel, userModel, careerModel
----------------------------------------------------------------
[~0:00 - 1:00] studentModel.js (backend/src/models/studentModel.js)
- Implemented CRUD queries for student, intern and graduate tables
- All three role types covered in a single file (shared app_user parent)
- Fixed 11 bugs across multiple review rounds:
  * Wrong parameter name in findStudentByUserId (user -> userId)
  * Typo in WHERE clause of updateStudent (st_id_user -> std_id_user)
  * Wrong table name in createIntern (inter -> intern)
  * Column name with space (itn_end date -> itn_end_date)
  * Typo in startDate parameter (starDate -> startDate)
  * Wrong column ap_usr_activate -> ap_usr_active in findInternByUserId
  * Missing comma after career_id alias in findInternByUserId
  * Wrong table name in LEFT JOIN (career -> careers)
  * Wrong WHERE column (it_id_user -> itn_id_user)
  * Double p typo in findGraduateByUserId (app_usr_id -> ap_usr_id)
  * Wrong alias in LEFT JOIN (au -> ua) in findGraduateByUserId
  * Typo in alias carrer_id -> career_id
  * findInterByUserId renamed to findInternByUserId (missing 'n')

[~1:00 - 1:45] userModel.js (backend/src/models/userModel.js)
- Implemented 8 queries for app_user table:
  findById, findByEmail, create, update, updateCvUrl,
  updateToken, verifyEmail, findByToken
- Fixed 3 bugs across review rounds:
  * pool.execute called with object {} instead of array []
  * Wrong column name ap_usr_career -> ap_usr_id_career
  * Trailing comma before WHERE in verifyEmail SQL
  * Duplicate ap_usr_email_verified column in findByEmail SELECT

[~1:45 - 2:00] careerModel.js (backend/src/models/careerModel.js)
- Implemented 2 queries for careers table: getAll, getById
- getAll returns careers ordered alphabetically for frontend dropdowns
- getById used for careerId validation in service layer

----------------------------------------------------------------
DATE: May 8th, 2026
AUTHOR: MiguelAGDev & AzucenaRF
SESSION TIME: ~3 hours
ACTIONS: ES Module migration, db.js refactor, authService
----------------------------------------------------------------

[~0:00 - 0:30] ES Module migration (entire backend)
- Converted all model files from CommonJS (require/module.exports)
  to ES Modules (import/export)
- Decided on named exports across all model files for consistency
- Fixed userModel.js which was using export default {} — changed
  to named export { } to match careerModel and studentModel
- Added "type": "module" to package.json

[~0:30 - 1:00] db.js refactor (backend/src/config/db.js)
- Converted from CommonJS to ES Modules
- Added named export execute() — thin wrapper around pool.execute()
  so models can call execute(sql, params) directly
- Added named export getConnection() — exposes raw pool connection
  for authService audit-log trigger support
- Removed export default pool, replaced with export { execute, getConnection }
- Removed dotenv.config() — moved responsibility to index.js

[~1:00 - 2:30] authService.js (backend/src/services/authService.js)
- Implemented 3 public functions: register, login, verifyEmail
- Implemented 3 private functions: _borrowConnection, _detectRole,
  _getPermissions (prefixed with _ convention = not exported)
- register: duplicate email check, bcrypt hash, userModel.create,
  role switch (student/intern/graduate), token generation, email send
- login: findByEmail, email_verified check, active check,
  bcrypt compare, role detection, permissions bitmask, JWT sign
- verifyEmail: findByToken, expiration check, markEmailVerified
- Company branch in register left as TODO (waiting for Azucena)
- role_history insert left as TODO (waiting for roleHistoryModel.js)
- Fixed 8 bugs across review rounds:
  * Duplicate imports: error from console and bcrypt (removed)
  * Register condition inverted (!existing -> existing)
  * Date.now missing parentheses (Date.now -> Date.now())
  * error.statusCode typo in login (error -> err) x2
  * Missing throw after wrong password check in login
  * bcrypt.compare called with bcrypt instead of bcryptjs
  * Default case using single quotes instead of backticks for ${role}
  * JWT_EXPIRE_IN typo fixed to JWT_EXPIRES_IN
  * Missing new before Error() in verifyEmail
  * rows[0].mask changed to rows[0]?.mask in _getPermissions

----------------------------------------------------------------
DATE: May 8th, 2026
AUTHOR: MiguelAGDev
SESSION TIME: ~2 hour
ACTIONS: authController, authRoutes, bug fixes
----------------------------------------------------------------

[~0:00 - 0:20] Bug fixes across existing files
- errorHandler.js: changed err.status to err.statusCode ?? err.status ?? 500
  to match the convention used in authService (statusCode not status)
- errorHandler.js: removed async keyword — Express v4 silently ignores
  async error handlers, uncaught rejections would bypass the handler
- mailer.js: changed export { transporter } to export default transporter
  to match the default import used in emailService.js
- careerModels.js, studentModel.js, userModel.js: added .js extension
  to db import — required by Node ESM ("type": "module")

[~0:20 - 1:00] authController.js (backend/src/controllers/authController.js)
- Implemented 3 handlers: registerHandler, loginHandler, verifyEmailHandler
- Thin HTTP layer only — reads req, calls authService, writes res
- All errors forwarded with next(err) to errorHandler
- Fixed loginHandler returning 201 instead of 200

[~1:00 - 2:00] authRoutes.js (backend/src/routes/authRoutes.js)
- Wired all 3 routes: POST /register, POST /login, GET /verify/:token
- Added validate() middleware using express-validator
- Role-conditional validation: semester/stdId for student,
  startDate/endDate for intern, graduationYear for graduate
- Fixed 6 bugs across review rounds:
  * router never declared (const router = Router() missing)
  * Missing / prefix on /register and /login routes
  * whithMessage typo → withMessage
  * isISO08601 typo → isISO8601 (twice)
  * statrDate typo → startDate
  * role isIn() had one string instead of four separate values

----------------------------------------------------------------
DATE: May 5th, 2026
AUTHOR: MiguelAGDev & AzucenaRF
SESSION TIME: ~2 hours
ACTIONS: Company, job posting, and application model implementation
----------------------------------------------------------------
[~0:00 - 0:45] Company Model
- Implemented company CRUD and approval workflow in backend/src/models/companyModel.js
- Added createCompany, findCompanyByUserId, updateCompany,
  updateCompanyApprovalStatus, getPendingCompanies
- Approval reject path stores rejection reason; approve/pending clears reason

[~0:45 - 1:30] Job Posting Model
- Implemented createJobPosting, findJobPostingById, findJobPostingsByCompany,
  updateJobPosting, updateJobPostingApprovalStatus, getPendingJobPostings
- Added career association helpers attachJobPostingCareers and detachJobPostingCareers
- Added public filtered listing with modality, contract type, experience,
  career and search support

[~1:30 - 2:00] Application Model
- Implemented createApplication, findApplicationsByUser,
  findApplicationsByJobPosting, findApplicationByUserAndJobPosting
- Added Roll Me / CV send logging and duplicate application prevention

----------------------------------------------------------------
DATE: May 17th, 2026
AUTHOR: MiguelAGDev & AzucenaRF
SESSION TIME: ~3 hours
ACTIONS: Company/job posting/application controllers and routes
----------------------------------------------------------------
[~0:00 - 1:30] Company features
- Built controller and routes for company registration, company profile retrieval,
  company update and admin approval actions
- Added strong request validation for company fields and approval status
- Added endpoints: POST /api/company, GET /api/company/me, PUT /api/company/me,
  GET /api/company/pending, PATCH /api/company/:userId/approval

[~1:30 - 2:30] Job posting features
- Built job posting controller and routes for posting creation, listing,
  detail retrieval, company-owned postings, updates and admin approval
- Added request validation for posting metadata and publication/expiration dates
- Added acceptance of careerIds to attach posting-career relationships

[~2:30 - 3:00] Application features
- Built application controller and routes for applying to jobs,
  fetching my applications, and listing applications by job posting
- Added validation for job_posting_id and company applicant queries

----------------------------------------------------------------
DATE: May 18th, 2026
AUTHOR: MiguelAGDev & AzucenaRF
SESSION TIME: ~1 hour
ACTIONS: Request validation and route stabilization
----------------------------------------------------------------
[~0:00 - 1:00] Validation middleware
- Implemented backend/src/middlewares/validateRequest.js
- Centralized express-validator error formatting and 422 response payloads
- Polished route validation chains in company, jobPosting, applications and user routes

----------------------------------------------------------------
DATE: June 8th, 2026
AUTHOR: MiguelAGDev & AzucenaRF
SESSION TIME: ~1.5 hours
ACTIONS: CV upload support and user file handling
----------------------------------------------------------------
[~0:00 - 1:30] CV upload middleware and user CV endpoint
- Implemented backend/src/middlewares/uploadMiddlewares.js with multer
- Enforced PDF-only CV uploads, 5MB limit, and uploads/cvs directory creation
- Added PUT /api/users/cv endpoint to save CV path in user profile
- Updated userModel.updateCvUrl and userController.updateMyCv

----------------------------------------------------------------
DATE: June 25th, 2026
AUTHOR: MiguelAGDev & AzucenaRF
SESSION TIME: ~2 hours
ACTIONS: User profile, Roll Me flow and email integration
----------------------------------------------------------------
[~0:00 - 1:00] User profile endpoints
- Implemented GET /api/users/me and PUT /api/users/me
- Added profile update with optional phone and careerId fields
- Added fresh user lookup, unchanged-profile response and success payloads

[~1:00 - 2:00] Roll Me email flow
- Implemented POST /api/users/roll-me to send CV to company contact email
- Added duplicate application prevention and approved posting check
- Integrated sendRollMeEmail service and application logging

----------------------------------------------------------------
DATE: June 28th, 2026
AUTHOR: MiguelAGDev & AzucenaRF
SESSION TIME: ~1 hour
ACTIONS: Role-based access control
----------------------------------------------------------------
[~0:00 - 1:00] Role middleware
- Implemented backend/src/middlewares/roleMiddleware.js
- Added single-role and multi-role checks with 401/403 handling
- Prepared protected route patterns for admin/company workflows

----------------------------------------------------------------
DATE: June 29th, 2026
AUTHOR: MiguelAGDev
SESSION TIME: ~1 hour
ACTIONS: Test scripts and final review
----------------------------------------------------------------
[~0:00 - 1:00] Test coverage and review
- Added backend/src/tests/testCompanyModel.js and testJobPostingModel.js
- Reviewed model query behaviors, approval validation, and route contracts
- Adjusted log to include all completed backend work through June

================================================================
TOTAL TIME INVESTED: ~31.5 hours
================================================================
